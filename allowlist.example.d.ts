/**
 * Declarations for the hand-written example allowlist.
 *
 * `allowlist.example.js` is deliberately plain JavaScript: it documents the shape a
 * deployment writes by hand in its own gitignored `allowlist.local.js`. This exists only
 * so the test that checks that shape can be type-checked alongside everything else.
 */
export declare const allow_list: string[];
